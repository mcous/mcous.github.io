---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'code'>

const className = 'font-mono rounded px-1 py-0.5 bg-slate-200'
---

<code class={className} {...Astro.props}><slot /></code>
